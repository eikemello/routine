<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".RoutineApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Routine">

        <service
            android:name=".listener.NotificationListener"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">

            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ConfigActivity"
            android:exported="false" />
        <activity
            android:name=".activity.TrackerConfigActivity"
            android:exported="false" />
        <activity
            android:name=".activity.HistoryActivity"
            android:exported="false" />

        <!-- Combined home screen widget: the water card (quick-add buttons, total,
             and progress) stacked over the meal card (four-slot segments with the
             meals already logged, the day's x/4 count and the Correct / Warning /
             Wrong status buttons), all in a single 4x3 widget. Not exported: the
             launcher gets to it through the system (explicit APPWIDGET_UPDATE
             broadcasts) and through this app's own PendingIntents. -->
        <receiver
            android:name=".widget.WidgetCombinedProvider"
            android:exported="false"
            android:label="@string/widget_water_meal_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.android.nls.routine.action.widget.ADD_WATER" />
                <action android:name="com.android.nls.routine.action.widget.SAVE_MEAL" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_water_meal_info" />
        </receiver>
    </application>

</manifest>
